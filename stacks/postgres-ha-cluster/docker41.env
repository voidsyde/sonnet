# docker01
NODE_NAME=docker01
NODE_ID=1

# Host IPs (consensus/replication on same subnet per APPLY)
HOST_REPL_IP=10.0.0.41
HOST_CONS_IP=10.0.0.41

# Patroni REST bind IP
PATRONI_REST_IP=${HOST_CONS_IP}

# etcd settings for 3-node (add 10.0.0.44 if using docker04 too)
ETCD_NAME=n1
ETCD_INITIAL_CLUSTER=n1=http://10.0.0.41:2380,n2=http://10.0.0.42:2380,n3=http://10.0.0.43:2380
ETCD_ENDPOINTS=10.0.0.41:2379,10.0.0.42:2379,10.0.0.43:2379

# PgBouncer static IP (macvlan) on client subnet (adjust as needed)
PGBOUNCER_IP=192.168.6.241

# Data paths (ZFS datasets should exist or be created by init script)
DATA_DIR=${ZFS_BASE}/${NODE_NAME}/pgdata
WAL_DIR=${ZFS_BASE}/${NODE_NAME}/pgwal
ETCD_DATA_DIR=${ZFS_BASE}/${NODE_NAME}/etcd

# Patroni candidate priority
PATRONI_CANDIDATE_PRIORITY=100
